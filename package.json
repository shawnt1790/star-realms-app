{
  "name": "starrealms-io",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm -ws run dev",
    "build": "npm -w @sr/shared run build && npm -w @sr/server run build && npm -w @sr/client run build",
    "test": "npm -ws run test",
    "lint": "eslint .",
    "typecheck": "npm -w @sr/shared run typecheck && npm -w @sr/server run typecheck && npm -w @sr/client run typecheck",
    "format": "prettier . --write",
    "format:check": "prettier . --check"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.54.0",
    "@typescript-eslint/parser": "^8.54.0",
    "eslint": "^9.39.2",
    "prettier": "^3.8.1"
  }
}
